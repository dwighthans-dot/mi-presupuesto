{
  "name": "mesada",
  "version": "12.12.0",
  "private": true,
  "description": "Mesada — Tu mes, bien organizado",
  "scripts": {
    "build": "node scripts/build-web.mjs",
    "web": "npm run build"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
