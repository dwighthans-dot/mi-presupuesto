# Android — Mesada V12.13

Esta carpeta queda reservada para la plataforma nativa Android generada por Capacitor.

No contiene una plataforma Android simulada. Debe generarse con `npx cap add android` después de ejecutar `npm install` y `npm run build` en un entorno con Android Studio/SDK.
